
import React from "react";
import {Container, ButtonStyle} from './styles'

const Button = ({
type,
name,
}) =>{
    return(
        <Container>
            <ButtonStyle>Авторизация</ButtonStyle>
        </Container>
    )
};

export default Button;